# AGENTS

## Workspace overview
This workspace currently has no source files or configuration files. Use this workspace as a starting point for the user’s `cline`-related project.

## How to help
- Ask the user for the intended functionality when they request `cline` support.
- If the user wants a new `cline` project, prompt for the programming language, target platform, and any integration details.
- Keep instructions minimal and focus on generating the initial files needed to start the project.

## Cline usage note
- `cline` appears to be the user’s target customization or custom agent integration.
- Prefer creating clear, scaffolded setup files and instructions rather than guessing project structure.
- If asked to add `cline` support, verify whether they want a custom agent config, CLI scaffolding, or workspace-level rules.
- For an empty workspace, do not start full implementation without confirming the user wants the scaffold created here.
- Refer to `.clinerules` for Cline-specific behavior and prompt-handling rules.
